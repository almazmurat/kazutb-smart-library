# draft.md

> **Minimal mode:** one line is enough.
> You can literally write only this:
>
> `улучшить фильтр каталога`
>
> Then run `/clarify` → `/design` → `/implement` → `/verify` → `/document`.
> The agent should generate the rest.

## Idea
- What should change?

## Optional notes
- What hurts now?
- What should the result feel like?
- Any constraints or boundaries?

## Example
- улучшить фильтр каталога
- reader should be able to filter by availability and year without breaking `/api/v1/catalog-db`
